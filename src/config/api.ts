// backend URL - change to your local IP when running locally, Render URL for production
export const API_BASE_URL = 'https://edgemarket-g119.onrender.com';

export const API_PREFIX = `${API_BASE_URL}/api`;
